# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# Résumé PDFs are never committed — see SPEC.md § Résumé. The file carried
# contact details and lived in git history, where deleting it later would not
# have removed it. Keep any local copy outside the repo.
public/resume*.pdf
*resume*.pdf

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
